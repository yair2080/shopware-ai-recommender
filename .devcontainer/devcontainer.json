{
  "name": "Shopware 6 Development",
  "image": "mcr.microsoft.com/devcontainers/universal:2-linux",
  "forwardPorts": [8000, 3306],
  "portsAttributes": {
    "8000": {
      "label": "Shopware",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y php8.1-cli php8.1-mbstring php8.1-xml php8.1-curl php8.1-zip php8.1-mysql unzip curl git && curl -sS https://getcomposer.org/installer | sudo php -- --install-dir=/usr/local/bin --filename=composer",
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "vue.volar",
        "xdebug.php-debug"
      ]
    }
  }
}
